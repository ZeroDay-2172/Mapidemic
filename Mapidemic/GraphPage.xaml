<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Mapidemic"
             xmlns:microcharts="clr-namespace:Microcharts.Maui;assembly=Microcharts.Maui"
             xmlns:chart="clr-namespace:Syncfusion.Maui.Toolkit.Charts;assembly=Syncfusion.Maui.Toolkit"
             xmlns:popup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
             x:Class="Mapidemic.GraphPage">

    <VerticalStackLayout Margin="0,0,20,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Text="Recent Trends for:"
                    FontSize="18"
                    Padding="20"
                    Grid.Row="0"
                    Grid.Column="0"/>
            <Picker Title="Select Illness"
                    WidthRequest="200"
                    HorizontalOptions="Start"
                    Margin="20"
                    Grid.Row="0"
                    Grid.Column="1"
                    x:Name="illnessPicker"
                    SelectedIndexChanged="IllnessChosen_handler"
                    ItemsSource="{Binding IllnessCollection}"
                    ItemDisplayBinding="{Binding Name}">
                <Picker.Items>
                </Picker.Items>
            </Picker>
        </Grid>

        <chart:SfCartesianChart x:Name="dataChart">
            <chart:SfCartesianChart.XAxes>
                <chart:CategoryAxis>
                    <chart:CategoryAxis.Title>
                        <chart:ChartAxisTitle Text="Date"/>
                    </chart:CategoryAxis.Title>
                </chart:CategoryAxis>
            </chart:SfCartesianChart.XAxes>
            <chart:SfCartesianChart.YAxes>
                <chart:NumericalAxis Interval="1">
                    <chart:NumericalAxis.Title>
                        <chart:ChartAxisTitle Text="# of cases"/>
                    </chart:NumericalAxis.Title>
                </chart:NumericalAxis>
            </chart:SfCartesianChart.YAxes>

            <chart:ColumnSeries x:Name="column"
                                ItemsSource="{Binding data}"
                                XBindingPath="dateString"
                                YBindingPath="value"/>
        </chart:SfCartesianChart>


        <Grid Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- View National data, or data from your zip code -->
            <Label Text="Locality:"
                    FontSize="18"
                    Grid.Row="0"
                    Grid.Column="0"/>
            <Picker Grid.Row="0" Grid.Column="1" x:Name="localityPicker" SelectedIndexChanged="LocalityPicked_handler" Title="National or Zip Code?">
                <Picker.Items>
                    <x:String>National</x:String>
                    <x:String>Zip Code</x:String>
                </Picker.Items>
            </Picker>

            <Label Text="Time Range:"
                    FontSize="18"
                    Grid.Row="1"
                    Grid.Column="0"/>
            <Picker Grid.Row="1" Grid.Column="1" x:Name="timeRangePicker" SelectedIndexChanged="TimeRangePicked_handler" Title="# of days">
                <Picker.Items>
                    <x:String>7 days</x:String>
                    <x:String>14 days</x:String>
                    <x:String>30 days</x:String>
                </Picker.Items>
            </Picker>

            <Button Text="Load Data"
                    TextColor="white"
                    FontSize="18"
                    BackgroundColor="#0074C0"
                    Clicked="RefreshButtonClicked"
                    Grid.Row="2"
                    Grid.Column="0"
                    Margin="20"/>
        </Grid>

        <popup:SfPopup Message=""
                       x:Name="Popup"
                       IsOpen="False"
                       HeaderTitle=""
                       StaysOpen="True"
                       HeaderHeight="0"
                       WidthRequest="125"
                       HeightRequest="125"
                       ShowCloseButton="False"
                       VerticalOptions="Center"
                       HorizontalOptions="Center">
                
                <popup:SfPopup.ContentTemplate>
                        <DataTemplate>
                                <ActivityIndicator IsRunning="True"
                                                   Color="#0074C0"
                                                   VerticalOptions="Center"
                                                   HorizontalOptions="Center"
                                                   Scale="2"/>
                        </DataTemplate>
                </popup:SfPopup.ContentTemplate>

        </popup:SfPopup>
        
    </VerticalStackLayout>


</ContentPage>